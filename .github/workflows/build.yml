# Visit https://github.com/lowlighter/metrics#-documentation for full reference
name: Metrics
on:
  # Schedule updates (each hour)
  schedule: [{cron: "0 17 * * 6"}]
  # Lines below let you run workflow manually and on each commit
  workflow_dispatch:
  push: {branches: ["master", "main"]}

jobs:
  github-metrics:
    runs-on: ubuntu-latest
    permissions:
      contents: write        # ให้สิทธิ์ push/commit ไฟล์ผลลัพธ์
      issues: read           # (แนะนำ) ถ้าปลั๊กอินอ่าน Issues ด้วย
      pull-requests: read    # (แนะนำ) ถ้าปลั๊กอินอ่าน PR ด้วย
      packages: read         # (แนะนำ) ถ้าดึงข้อมูล packages
    steps:
      - uses: lowlighter/metrics@latest
        with:
          # ใช้ PAT ของคุณสำหรับเรียก GitHub API (อ่านข้อมูลได้ครบกว่า)
          token: ${{ secrets.METRICS_TOKEN }}

          # ใช้ GITHUB_TOKEN ของ workflow สำหรับ commit/push ไฟล์กลับเข้าที่ repo
          # (คีย์ที่ถูกต้องคือ committer_token)
          committer_token: ${{ secrets.GITHUB_TOKEN }}

          # Options
          template: classic
          base: header, activity, community, repositories, metadata
          config_octicon: yes
          config_timezone: Asia/Bangkok
          config_twemoji: yes
          plugin_fortune: yes
          plugin_isocalendar: yes
          plugin_isocalendar_duration: full-year
          plugin_languages: yes
          plugin_languages_analysis_timeout: 15
          plugin_languages_analysis_timeout_repositories: 7.5
          plugin_languages_categories: markup, programming
          plugin_languages_colors: github
          plugin_languages_limit: 6
          plugin_languages_recent_categories: markup, programming
          plugin_languages_recent_days: 14
          plugin_languages_recent_load: 300
          plugin_languages_sections: most-used
          plugin_languages_threshold: 0%
          plugin_lines: yes
          plugin_lines_history_limit: 1
          plugin_lines_repositories_limit: 4
          plugin_lines_sections: base
          plugin_topics: yes
          plugin_topics_limit: 15
          plugin_topics_mode: icons
          plugin_topics_sort: stars
